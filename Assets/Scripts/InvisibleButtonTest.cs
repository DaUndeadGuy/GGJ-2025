using UnityEngine;

public class InvisibleButtonTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    public void Clicked()
    {
        Debug.Log("Click");
    }
}
